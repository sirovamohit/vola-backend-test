export enum TransactionType {
    DEBIT = 'debit',
    CREDIT = 'credit',
}

export enum TransactionStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    FAILED = 'failed',
    REFUNDED = 'refunded',
}

export enum CardStatus {
    ACTIVE = 'active',
    BLOCKED = 'blocked',
    EXPIRED = 'expired',
}

